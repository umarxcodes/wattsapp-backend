const first=3
console.log(first);

// This will cause a linting error - unused variable
const unusedVariable = "this is not used";

// This will cause a formatting error - wrong indentation
  console.log("bad indentation");
